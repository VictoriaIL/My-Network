const db = require('../config/db-config');

module.exports.overview = function (req, res) {
  // const users = db.query('Select * from Users');
  // users.then(result => res.status(200).json(result.rows));
};
